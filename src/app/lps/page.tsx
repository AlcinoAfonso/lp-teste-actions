import Link from 'next/link';  interface LPInfo {   cliente: string;   title: string;   description: string;   url: string;   status: string;   type: string; }  // ✅ ETAPA 1 - Lista estática de LPs funcionais async function getAllLandingPages(): Promise<LPInfo[]> {   // Sistema migrado para estático - lista hardcoded das LPs funcionais   const landingPages: LPInfo[] = [     {       cliente: 'lp-factory',       title: 'LP Factory - Criação de Landing Pages de Alta Conversão | A partir de R$ 497',       description: 'Transforme visitantes em clientes com landing pages profissionais. Design moderno, carregamento ultra-rápido e otimização para conversão. Entrega em 48h.',       url: '/',       status: '✅ Ativa',       type: 'Showcase'     },     {       cliente: 'fitnutri',       title: 'FitNutri - Transforme seu Corpo em 90 Dias | Nutrição Personalizada',       description: 'Perca peso de forma saudável com acompanhamento nutricional personalizado. Cardápios exclusivos, suporte 24h e resultados garantidos.',       url: '/fitnutri',       status: '✅ Ativa',       type: 'Cliente'     }   ];    return landingPages.sort((a, b) => a.cliente.localeCompare(b.cliente)); }  export const metadata = {   title: 'Landing Pages Ativas - LP Factory',   description: 'Lista de todas as landing pages funcionais criadas pela LP Factory', };  export default async function LandingPagesListPage() {   const landingPages = await getAllLandingPages();      return (     <div className="min-h-screen bg-gray-50 py-12">       <div className="container-lp">         <h1 className="text-4xl font-bold text-center mb-12">           🏭 Landing Pages Funcionais         </h1>                  {landingPages.length === 0 ? (           <p className="text-center text-gray-600">             Nenhuma landing page encontrada.           </p>         ) : (           <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-1 gap-6 max-w-4xl mx-auto">             {landingPages.map((lp) => (               <Link                 key={lp.cliente}                 href={lp.url}                 className="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow p-6 block border-l-4 border-green-500"               >                 <div className="flex justify-between items-start mb-3">                   <div>                     <h2 className="text-xl font-semibold text-gray-900">{lp.cliente}</h2>                     <span className="text-xs text-gray-500 bg-gray-100 px-2 py-1 rounded-full">                       {lp.type}                     </span>                   </div>                   <span className="text-sm bg-green-100 text-green-800 px-2 py-1 rounded-full">                     {lp.status}                   </span>                 </div>                 <h3 className="text-sm font-medium text-gray-600 mb-2">{lp.title}</h3>                 <p className="text-sm text-gray-500 mb-4">                   {lp.description}                 </p>                 <span className="text-sm text-blue-600 hover:text-blue-800 font-medium">                   Ver Landing Page →                 </span>               </Link>             ))}           </div>         )}                  <div className="mt-12 text-center">           <Link             href="/"             className="text-gray-600 hover:text-gray-800 underline"           >             ← Voltar para a página inicial           </Link>         </div>       </div>     </div>   ); }
